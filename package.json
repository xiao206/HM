{
  "name": "xsy",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && npx wrangler deploy",
    "deploy:main": "node scripts/deploy.mjs",
    "deploy:main:dry": "node scripts/deploy.mjs --dry-run"
  },
  "dependencies": {
    "medium-zoom": "^1.1.0",
    "vue": "^3.5.32"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.6",
    "vite": "^8.0.10"
  }
}
